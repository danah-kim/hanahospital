import React from "react";
import styled from "styled-components";

const ContactContainer = styled.div``;

const Contact = () => (
  <ContactContainer id="contact">상담문의</ContactContainer>
);

export default Contact;
